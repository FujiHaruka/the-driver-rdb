/**
 * Test for parseOutboundValues.
 * Runs with mocha.
 */
'use strict'

const parseOutboundValues = require('../lib/parsing/parseOutboundValues')
const { ok, equal } = require('assert')

describe('parse-outbound-values', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

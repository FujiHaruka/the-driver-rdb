/**
 * Test for parseFilter.
 * Runs with mocha.
 */
'use strict'

const parseFilter = require('../lib/parsing/parseFilter')
const { ok, equal } = require('assert')

describe('parse-filter', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

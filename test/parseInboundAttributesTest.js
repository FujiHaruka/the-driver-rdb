/**
 * Test for parseInboundAttributes.
 * Runs with mocha.
 */
'use strict'

const parseInboundAttributes = require('../lib/parsing/parseInboundAttributes')
const { ok, equal } = require('assert')

describe('parse-inbound-attributes', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

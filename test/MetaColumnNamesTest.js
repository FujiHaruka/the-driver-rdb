/**
 * Test for MetaColumnNames.
 * Runs with mocha.
 */
'use strict'

const MetaColumnNames = require('../lib/constants/MetaColumnNames')
const { ok, equal } = require('assert')

describe('meta-column-names', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

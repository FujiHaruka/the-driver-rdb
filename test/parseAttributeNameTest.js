/**
 * Test for parseAttributeName.
 * Runs with mocha.
 */
'use strict'

const parseAttributeName = require('../lib/parsing/parseAttributeName')
const { ok, equal } = require('assert')

describe('parse-attribute-name', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

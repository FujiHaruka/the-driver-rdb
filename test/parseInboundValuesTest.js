/**
 * Test for parseInboundValues.
 * Runs with mocha.
 */
'use strict'

const parseInboundValues = require('../lib/parsing/parseInboundValues')
const { ok, equal } = require('assert')

describe('parse-inbound-values', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

/**
 * Test for parseSort.
 * Runs with mocha.
 */
'use strict'

const parseSort = require('../lib/parsing/parseSort')
const { ok, equal } = require('assert')

describe('parse-sort', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

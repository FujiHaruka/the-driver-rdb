/**
 * Test for parseOutboundAttributes.
 * Runs with mocha.
 */
'use strict'

const parseOutboundAttributes = require('../lib/parsing/parseOutboundAttributes')
const { ok, equal } = require('assert')

describe('parse-outbound-attributes', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

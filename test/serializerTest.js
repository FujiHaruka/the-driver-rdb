/**
 * Test for serializer.
 * Runs with mocha.
 */
'use strict'

const serializer = require('../lib/helpers/serializer')
const { ok, equal } = require('assert')

describe('serializer', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

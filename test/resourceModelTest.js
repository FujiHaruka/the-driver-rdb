/**
 * Test for resourceModel.
 * Runs with mocha.
 */
'use strict'

const resourceModel = require('../lib/modeling/resourceModel')
const { ok, equal } = require('assert')

describe('resource-model', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

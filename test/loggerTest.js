/**
 * Test for logger.
 * Runs with mocha.
 */
'use strict'

const logger = require('../lib/helpers/logger')
const { ok, equal } = require('assert')

describe('logger', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

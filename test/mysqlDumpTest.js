/**
 * Test for mysqlDump.
 * Runs with mocha.
 */
'use strict'

const mysqlDump = require('../lib/dumps/mysqlDump')
const { ok, equal } = require('assert')

describe('mysql-dump', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

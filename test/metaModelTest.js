/**
 * Test for metaModel.
 * Runs with mocha.
 */
'use strict'

const metaModel = require('../lib/modeling/metaModel')
const { ok, equal } = require('assert')

describe('meta-model', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

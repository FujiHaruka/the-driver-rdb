/**
 * Test for sequelizeMix.
 * Runs with mocha.
 */
'use strict'

const sequelizeMix = require('../lib/mixins/sequelizeMix')
const { ok, equal } = require('assert')

describe('sequelize-mix', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */

/**
 * Test for extraModel.
 * Runs with mocha.
 */
'use strict'

const extraModel = require('../lib/modeling/extraModel')
const { ok, equal } = require('assert')

describe('extra-model', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
